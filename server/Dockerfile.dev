# Development image — ts-node-dev with hot reload.
# Source is bind-mounted by docker-compose; node_modules stay in the image.
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
EXPOSE 5000
CMD ["npm", "run", "dev"]
